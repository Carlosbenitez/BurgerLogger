<h1>Burger Logger</h1>

<h2>Saved Burgers:</h2>

<ul class="saved">
  {{#each burger}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have been DEVOURED:</h2>

<ul class="devoured">
  {{#each burger}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="bu" id="nameForm">Burger Name:</label>
    <input type="text" id="bu" burger_name="burger_name">
  </div>

  <div class="form-group">
    <label for="devoured" id="devourForm">DEVOUR or save for later!?</label><br>
    <input type="radio" burger_name="devoured" name='re' value="1"> DEVOURED!<br>
    <input type="radio" burger_name="devoured" name='re' value="0"> Save!
  </div>

  <button type="radio">Add Burger</button>
</form>
