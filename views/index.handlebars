<h1>Burgers!</h1>

<h2>Saved Burgers.</h2>

<ul>
  {{#each burger}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have been DEVOURED!</h2>

<ul>
  {{#each burger}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="bu">Burger Name:</label>
    <input type="text" id="bu" burger_name="burger_name">
  </div>

  <div class="form-group">
    <label for="devoured">Time to DEVOUR!?</label><br>
    <input type="radio" burger_name="devoured" value="1" checked> DEVOURED!<br>
    <input type="radio" burger_name="devoured" value="0"> Save!
  </div>

  <button type="submit">Add Burger</button>
</form>
